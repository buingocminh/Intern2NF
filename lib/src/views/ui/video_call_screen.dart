import 'package:flutter/material.dart';

class video_call_screen extends StatelessWidget {
  const video_call_screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(
          child: Text("đoạn video call chưa biết làm như nào :))"),
        ),
      ),
    );
  }
}
